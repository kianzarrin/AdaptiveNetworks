- hint for vanilla flags/drop-downs
    - hoc to modify game flags
- decide in which direction I need to layout the segment's start/end node/segmentEnd flags. should it be influenced by invert?
- 

version 2:
    mixed enum type bitmask
    custom flags
        data manager to manage them.
        UI to control them.
        all the network persistancy features.