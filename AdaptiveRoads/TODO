backend can handle all sort of hints. need to add hints for:
    vanilla flags/drop-downs
    my own flags/drop-downs.

version 2:
    mixed enum type bitmask
    custom flags
        data manager to manage them.
        UI to control them.
        all the network persistancy features.
